server:
  port: 8080

spring:
  application:
    name: customer
  datasource:
    username: 'pei'
    url: jdbc:postgresql://localhost:5332/customer
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: true