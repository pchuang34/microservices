server:
  port: 8081

spring:
  application:
    name: fraud
  datasource:
    username: 'pei'
    url: jdbc:postgresql://localhost:5332/fraud
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: true